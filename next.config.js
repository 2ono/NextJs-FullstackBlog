/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
        "pixabay.com",
        "cdn.pixabay.com",
        "avatars.githubusercontent.com"
    ],
  },
};

module.exports = nextConfig;
